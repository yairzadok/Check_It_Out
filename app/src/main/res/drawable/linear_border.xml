<!-- res/drawable/linear_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray"/> <!-- Border width and color -->
    <corners android:radius="4dp"/> <!-- Rounded corners (optional) -->
</shape>
